# Root Procfile for DigitalOcean App Platform when Source = repository root.
# The backend needs the 'shared' package (sibling of backend/); backend/start.sh
# sets PYTHONPATH to the repo root so 'shared' can be imported.
web: sh backend/start.sh
