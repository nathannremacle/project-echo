import { Typography } from '@mui/material';

export default function Channels() {
  return (
    <div>
      <Typography variant="h4" gutterBottom>
        Channels
      </Typography>
      <Typography variant="body1" color="text.secondary">
        Channel list will be displayed here.
      </Typography>
    </div>
  );
}
